// break =  breaks out of a loop entirely
// continue = skip an iteration of loop

/* let count;
for(count = 34; count<=99; count+=10)
{
   if(count==54)
    break;
    console.log("break the statement")
}
*/

let count;
for(count = 34; count<=99; count+=10)
{
   if(count==54)
   continue;
    console.log("break the statement");
}
